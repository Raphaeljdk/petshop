{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "bunx prisma db push --schema=prisma/schema.prisma --skip-generate && bunx prisma generate --schema=prisma/schema.prisma && next build && cp -r .next/static .next/standalone/.next/ && cp -r public .next/standalone/"
}
